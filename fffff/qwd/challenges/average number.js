let averageNumber(1+3+5+7) /4;
 result (1+15) / 2; 
 var console:console=("Średnia: 15")
 getComputedStyle.apply(Screen:branch[DynamicsCompressorNode]
    ){
        console.log 
    }
    return 0
    <div id="game-row-top"><div class="grid-cell leaderboard-grid-cell"></div></div>    
    <dir>directly common for (let index = 0; index < array.length; index++) {
        const element = array[index];
        
    }</dir>